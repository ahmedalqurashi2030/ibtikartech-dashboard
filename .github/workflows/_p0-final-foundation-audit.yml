name: P0 one-time final foundation patch

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/_p0-final-foundation-audit.yml"

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: [self-hosted, production, ibtikartech]
    timeout-minutes: 10
    steps:
      - name: Checkout foundation branch
        uses: actions/checkout@v4
        with:
          ref: fix/p0-final-foundation-qa
          fetch-depth: 0

      - name: Apply verified foundation cleanup
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/_p0_final_foundation_patch.py
          # Workflow files require a higher GitHub App permission. Keep that
          # change out of this runner push; it is applied directly afterward.
          git checkout -- .github/workflows/django-clean-url-browser-qa.yml
          git diff --check

      - name: Commit non-workflow foundation changes
        shell: bash
        run: |
          set -euo pipefail
          rm scripts/_p0_final_foundation_patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix: finalize shared public foundation"
          git push origin HEAD:fix/p0-final-foundation-qa
